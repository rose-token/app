{
  "name": "rose-token",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "update-abi": "hardhat compile && node scripts/update-abi.js",
    "deploy:sepolia": "hardhat run scripts/update-deploy.js --network sepolia",
    "build": "cd frontend && npm install && npm run build",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.18.2",
    "@octokit/rest": "^20.0.2",
    "ethers": "^6.9.0",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.3.0",
    "hardhat": "^2.24.0",
    "nodemon": "^3.0.2"
  }
}
